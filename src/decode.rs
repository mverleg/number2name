use crate::Charset;
use crate::typ::N2NErr;

pub fn name2number<'a>(text: impl Into<&'a str>, charset: &Charset) -> Result<u64, N2NErr> {
    unimplemented!()
}

#[cfg(test)]
mod tests {

    #[test]
    fn demo() {
        unimplemented!()
    }
}
